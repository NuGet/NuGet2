<UserControl x:Class="NuGet.Client.VisualStudio.UI.BusyControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:resx="clr-namespace:NuGet.Client.VisualStudio.UI.Resources"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Rectangle Fill="Gray" Opacity="0.5" x:Name="_rectangle">
        </Rectangle>
        
        <Grid VerticalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <ProgressBar Height="20" IsIndeterminate="True" Grid.Row="0"></ProgressBar>
            <TextBlock Grid.Row="0" 
                       Text="{x:Static resx:Resources.Text_Working}"
                       HorizontalAlignment="Center" 
                       x:Name="_text"/>
            <Button Grid.Row="1" Margin="0,10,0,0" HorizontalAlignment="Center" Padding="20,0" 
                    Visibility="Collapsed" 
                    Content="{x:Static resx:Resources.Button_Cancel}"
                    Click="CancelButton_Click" x:Name="_cancelButton"/>
        </Grid>
    </Grid>
</UserControl>
