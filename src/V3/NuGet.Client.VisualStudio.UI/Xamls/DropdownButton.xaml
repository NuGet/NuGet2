<UserControl x:Class="NuGet.Client.VisualStudio.UI.DropdownButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid x:Name="_grid">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>  
        <Button Grid.Column="0" x:Name="_mainButton" Padding="4,0" Click="_mainButton_Click"></Button>
        <Button Grid.Column="1" x:Name="_dropdownButton" Click="_dropdownButton_Click" >
            <TextBlock x:Name="_downArrow">&#x25bc;</TextBlock></Button>
    </Grid>
</UserControl>
